# Rust
/target
# zkVM (separate workspace) build outputs, incl. the detached guest's target
zkvm/target/
zkvm/methods/guest/target/
**/*.rs.bk
Cargo.lock.orig

# Node / pnpm
node_modules/
dist/
*.tsbuildinfo
.pnpm-store/

# Env / secrets
.env
.env.*
!.env.example

# Internal planning notes — not for the public repo
reference/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
