[toolchain]
# Edition 2024 requires Rust 1.85 or newer. "stable" satisfies this on any
# reasonably up-to-date install; the workspace `rust-version` enforces the floor.
channel = "stable"
components = ["rustfmt", "clippy"]
