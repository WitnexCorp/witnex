{
  "name": "witnex-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Witnex — verifiable AI agent framework (TypeScript workspace root).",
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "lint": "eslint .",
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test"
  },
  "engines": {
    "node": ">=22.13"
  },
  "packageManager": "pnpm@11.7.0",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.5.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.10.0"
  }
}
